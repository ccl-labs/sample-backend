apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: sample-backend
  description: FastAPI + PostgreSQL によるサンプル API
  annotations:
    github.com/project-slug: ccl-labs/sample-backend
  tags:
    - python
    - fastapi
    - postgresql
spec:
  type: service
  lifecycle: experimental
  owner: user:platform-admin
  system: platform
